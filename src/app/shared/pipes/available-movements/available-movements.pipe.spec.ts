import { AvailableMovementsPipe } from './available-movements.pipe';

describe('AvailableMovementsPipe', () => {
  it('create an instance', () => {
    const pipe = new AvailableMovementsPipe();
    expect(pipe).toBeTruthy();
  });
});
